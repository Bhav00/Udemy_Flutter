import 'package:flutter/material.dart';
// import 'package:google_fonts/google_fonts.dart';

import 'package:evalaton/bin_go/playaround_ui.dart';

void main() {
  // right now it calls a stateless function,
  // Later it will need a function to manage the state of the app
  runApp(const MaterialApp(home: Thing()));
}
